# Практическая работа 24. Загрузка и отображение изображений из интернета

В лекциях 20 и 21 вы научились делать сетевые запросы и построили слой данных, который выводил количество загруженных фотографий. В этой работе вы завершите приложение **Mars Photos**: загрузите снимки Марса из открытого архива NASA и покажете их сеткой, добавив обработку состояний загрузки и ошибок.

### Пререквизиты

- Лекции 20 и 21 «Сетевые запросы» и «Паттерн репозиторий».
- Умение выполнять запрос через `fetch` и разбирать JSON-ответ.
- Умение выводить списки через `FlatList`.

### Что вы узнаете

- Как отобразить изображение по адресу из интернета.
- Как построить сетку с помощью `FlatList` и `numColumns`.
- Как обрабатывать состояния загрузки, успеха и ошибки, а при неудаче повторять запрос по кнопке.

### Что вы создадите

Приложение **Mars Photos** — сетка настоящих фотографий Марса из открытого архива изображений NASA, с индикатором загрузки и кнопкой «Повторить» при ошибке.

## Шаг 1. Подготовка проекта

```bash
npx create-expo-app@latest mars-photos --template blank-typescript
cd mars-photos
npx expo start
```

Если проект `mars-photos` уже создан в лекциях 20 и 21, просто откройте его — все изменения вносятся в файл `App.tsx`, а код в этой работе самодостаточный.

## Шаг 2. Адрес сервиса и формат ответа

В лекции 20 мы тренировались на jsonplaceholder — простом сервисе с плоским JSON-ответом. Теперь загрузим настоящие снимки: их отдаёт открытый архив изображений NASA. Адрес ресурса:

```tsx
const API_URL = 'https://images-api.nasa.gov/search?q=mars&media_type=image&page_size=20'
```

Ответ архива устроен сложнее: результаты поиска лежат внутри `collection.items`, а данные и ссылка на изображение разнесены по разным полям:

```json
{
  "collection": {
    "items": [
      {
        "data": [{ "title": "Mars Celebration" }],
        "links": [
          { "href": "https://images-assets.nasa.gov/image/NHQ201906010007/NHQ201906010007~medium.jpg" }
        ]
      }
    ]
  }
}
```

- `collection.items` — массив найденных снимков;
- `item.data[0].title` — название снимка;
- `item.links[0].href` — прямая ссылка на изображение;
- у некоторых результатов поля `links` может не быть — такие пропускаем.

Преобразовать такую вложенную структуру в удобный массив объектов `MarsPhoto` — задача функции загрузки из следующего шага: в сетке уже не придётся разбирать вложенность.

## Шаг 3. Тип данных и состояния экрана

Опишем тип, в который преобразуем ответ архива, и все три ситуации объединением типов — так же, как в лекции 20:

```tsx
type MarsPhoto = {
  id: number
  title: string
  img_src: string
}

type UiState =
  | { status: 'loading' }
  | { status: 'success'; photos: MarsPhoto[] }
  | { status: 'error'; message: string }
```

Функция загрузки не только получает данные, но и сразу преобразует вложенный ответ в плоский массив `MarsPhoto`:

```tsx
async function fetchPhotos(): Promise<MarsPhoto[]> {
  const response = await fetch(API_URL)

  if (!response.ok) {
    throw new Error(`Сервер вернул код ${response.status}`)
  }

  const json = await response.json()

  return json.collection.items
    .filter((item: any) => item.links && item.links.length > 0)
    .slice(0, 20)
    .map((item: any, index: number) => ({
      id: index,
      title: item.data[0].title as string,
      img_src: item.links[0].href as string,
    }))
}
```

Разберём преобразование по шагам:

- `json.collection.items` — достаём массив результатов из вложенного ответа;
- `.filter(...)` — оставляем только записи с полем `links`, остальные пропускаем;
- `.slice(0, 20)` — берём первые 20 снимков (столько запросили в `page_size`);
- `.map(...)` — каждый результат превращаем в объект `MarsPhoto`: `id` — порядковый номер, `title` — название снимка, `img_src` — прямая ссылка на картинку.

Название снимка сохраняется в поле `title` — в этой работе сетка показывает только изображения, но название пригодится в следующих работах.

## Шаг 4. Сетка изображений

Изображение по адресу выводит встроенный компонент **`Image`**. Ему передают объект `source` с полем `uri` — компонент сам скачивает картинку, кэширует её и показывает по мере загрузки:

```tsx
<Image source={{ uri: photo.img_src }} style={styles.image} />
```

Стиль ячейки задаёт её форму и фон до появления картинки:

- `aspectRatio: 1` делает ячейку квадратной — фотографии не «прыгают» при загрузке;
- `backgroundColor` виден, пока изображение загружается;
- `borderRadius` скругляет углы.

`FlatList` строит сетку, если задать число колонок. На узком экране удобны две колонки, на широком — четыре, поэтому посчитаем их по ширине окна:

```tsx
function PhotoGrid({ photos }: { photos: MarsPhoto[] }) {
  const { width } = useWindowDimensions()
  const numColumns = width > 600 ? 4 : 2

  return (
    <FlatList
      key={numColumns}
      data={photos}
      numColumns={numColumns}
      keyExtractor={(item) => item.id.toString()}
      renderItem={({ item }) => <Image source={{ uri: item.img_src }} style={styles.image} />}
    />
  )
}
```

> `key={numColumns}` обязателен: `FlatList` не умеет менять число колонок на лету, поэтому при смене ширины окна список пересоздаётся.

`FlatList` ленивый: он создаёт ячейки только для видимой части экрана, поэтому изображения догружаются по мере прокрутки.

## Шаг 5. Полный код App.tsx

```tsx
import { StatusBar } from 'expo-status-bar'
import { useEffect, useState } from 'react'
import {
  ActivityIndicator,
  Button,
  FlatList,
  Image,
  StyleSheet,
  Text,
  useWindowDimensions,
  View,
} from 'react-native'

type MarsPhoto = {
  id: number
  title: string
  img_src: string
}

type UiState =
  | { status: 'loading' }
  | { status: 'success'; photos: MarsPhoto[] }
  | { status: 'error'; message: string }

const API_URL = 'https://images-api.nasa.gov/search?q=mars&media_type=image&page_size=20'

async function fetchPhotos(): Promise<MarsPhoto[]> {
  const response = await fetch(API_URL)

  if (!response.ok) {
    throw new Error(`Сервер вернул код ${response.status}`)
  }

  const json = await response.json()

  return json.collection.items
    .filter((item: any) => item.links && item.links.length > 0)
    .slice(0, 20)
    .map((item: any, index: number) => ({
      id: index,
      title: item.data[0].title as string,
      img_src: item.links[0].href as string,
    }))
}

function PhotoGrid({ photos }: { photos: MarsPhoto[] }) {
  const { width } = useWindowDimensions()
  const numColumns = width > 600 ? 4 : 2

  return (
    <FlatList
      key={numColumns}
      data={photos}
      numColumns={numColumns}
      keyExtractor={(item) => item.id.toString()}
      contentContainerStyle={styles.grid}
      renderItem={({ item }) => <Image source={{ uri: item.img_src }} style={styles.image} />}
    />
  )
}

function MarsPhotosScreen() {
  const [state, setState] = useState<UiState>({ status: 'loading' })

  const loadPhotos = () => {
    setState({ status: 'loading' })

    fetchPhotos()
      .then((photos) => setState({ status: 'success', photos }))
      .catch((error: unknown) =>
        setState({
          status: 'error',
          message: error instanceof Error ? error.message : 'Не удалось загрузить данные',
        }),
      )
  }

  useEffect(() => {
    loadPhotos()
  }, [])

  if (state.status === 'loading') {
    return (
      <View style={styles.center}>
        <ActivityIndicator size="large" color="#0057B8" />
      </View>
    )
  }

  if (state.status === 'error') {
    return (
      <View style={styles.center}>
        <Text style={styles.error}>Не удалось загрузить фотографии</Text>
        <Text style={styles.message}>{state.message}</Text>
        <Button title="Повторить" onPress={loadPhotos} />
      </View>
    )
  }

  return (
    <View style={styles.screen}>
      <Text style={styles.title}>Mars Photos</Text>
      <PhotoGrid photos={state.photos} />
    </View>
  )
}

export default function App() {
  return (
    <>
      <MarsPhotosScreen />
      <StatusBar style="auto" />
    </>
  )
}

const styles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: '#F5F5F5', paddingTop: 48 },
  center: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 24, gap: 12 },
  title: { fontSize: 24, fontWeight: 'bold', paddingHorizontal: 16 },
  error: { fontSize: 18, color: '#C62828', textAlign: 'center' },
  message: { color: '#555555', textAlign: 'center' },
  grid: { padding: 4, paddingBottom: 24 },
  image: {
    flex: 1,
    aspectRatio: 1,
    margin: 4,
    borderRadius: 8,
    backgroundColor: '#E0E0E0',
  },
})
```

## Проверьте работу

1. Запустите приложение — сначала появится индикатор загрузки, затем сетка фотографий Марса.
2. Прокрутите список вниз — новые изображения догружаются по мере появления на экране.
3. Сузьте и расширьте окно браузера — число колонок изменится с двух на четыре.
4. Временно испортите адрес `API_URL` — появится сообщение об ошибке; верните адрес и нажмите «Повторить» — сетка восстановится.

## Задание

1. Покажите в заголовке количество загруженных фотографий.
2. Добавьте к `FlatList` свойство `refreshControl` с компонентом `RefreshControl` — потяните список вниз, чтобы обновить фотографии.
3. Перенесите загрузку в репозиторий и источник данных из лекции 21, а экран подключите через `useMarsPhotos`.
4. Проверьте поведение при отключённом интернете: приложение не должно «зависать», а сообщение об ошибке должно быть понятным.

## Резюме

- `Image` загружает изображение по `uri` и кэширует его — дополнительная библиотека не нужна.
- `FlatList` с `numColumns` строит сетку; при смене числа колонок список пересоздают через `key`.
- `aspectRatio` и `backgroundColor` делают сетку аккуратной даже во время загрузки картинок.
- Состояния загрузки, успеха и ошибки описывают объединением типов.
- Ошибку можно исправить повтором запроса — той же функцией загрузки по кнопке.
- `FlatList` ленивый: он создаёт только видимые ячейки, поэтому большие списки не тормозят.
